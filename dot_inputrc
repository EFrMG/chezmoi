# Enable vi mode
set editing-mode vi

# Cursor shape
set vi-ins-mode-string "\1\e[6 q\2"
set vi-cmd-mode-string "\1\e[2 q\2"

# Faster key response (delay after pressing Esc)
set keyseq-timeout 10

# Color files by type in completion
set colored-stats on
